# Skills

> [English](./README.md) · 繁體中文

一套供 Claude Code 與其他相容 Agent-Skills 標準的 harness 使用的 agent skills（slash commands 與行為）集合。

## 目錄結構

Skills 放在 `skills/<bucket>/<skill-name>/SKILL.md` 下。Bucket 分類：

- `engineering/` — 日常程式碼工作
- `productivity/` — 日常非程式碼工作流程工具
- `misc/` — 保留但很少用，不列入推廣
- `personal/` — 綁定個人環境設定，不列入推廣
- `in-progress/` — 草稿，尚未準備好上線
- `deprecated/` — 已不再使用

`engineering/` 與 `productivity/` 是**推廣中**的 bucket——裡面每個 skill 都會登錄在這份 README 與 `.claude-plugin/plugin.json` 裡。完整慣例請見 `CLAUDE.md`。

## 本機開發

```bash
scripts/link-skills.sh
```

把 `skills/` 底下每個 skill 都 symlink 到 `~/.claude/skills` 與 `~/.agents/skills`，方便本機測試。新增、刪除或重新命名 skill 後要重新執行。

## 參考

Skills 以一個軸線區分：由誰呼叫。**User-invoked（使用者呼叫）** 只有輸入時才會觸發，負責編排流程。**Model-invoked（模型呼叫）** 可由你呼叫，也可在情境符合時由 agent 自動觸發。User-invoked skill 可以呼叫 model-invoked skill，但不能呼叫另一個 user-invoked skill。

目前尚未登錄任何 skill——請見 `skills/` 底下各 bucket 自己的 `README.md`。
